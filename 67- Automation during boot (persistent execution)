Automation during boot (persistent execution)
Example concept
Cron jobs and system services can execute tasks automatically at startup.
Example: enable service at boot
sudo systemctl enable <service_name>
Tasks can run automatically when system starts.
guu
hhgg
